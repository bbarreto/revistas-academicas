<div class="container">
  <div class="row">
    <div class="col">
      <h2 class="mb-4">Bem-vindo</h2>
      {{#each universidades}}
      <div class="card mb-2">
        <div class="card-body">
          <h3><a href="/{{@key}}">{{title}}</a></h3>
          <p class="lead">{{description}}</p>
        </div>
      </div>
      {{/each}}
    </div>
  </div>
</div>
